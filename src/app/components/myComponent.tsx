import React from 'react';

// Define a functional component using JSX and TypeScript
function MyComponent(): JSX.Element {
  return (
    <div>
      <h1>Hello, World!</h1>
      <p>This is a simple JSX component with TypeScript.</p>
    </div>
  );
}

export default MyComponent;